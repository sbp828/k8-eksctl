apiVersion: v1
kind: Pod

metadata:
  name: nginx
  labels:
   name: service-test
   environment: dev
   project: localhelp
   component: frontend

spec:
 containers:
 - name: nginx
   image: nginx