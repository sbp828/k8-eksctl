apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: localhelp
  region: us-east-1

managedNodeGroups:
  - name: ng1
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 1